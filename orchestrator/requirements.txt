# Model APIs
openai>=1.40.0              # used only as an OpenAI-compatible client for NVIDIA
anthropic>=0.40.0           # Claude worker + display agents

# Web server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0

# Tiger Data (Timescale is Postgres, so this is just a Postgres driver)
psycopg[binary,pool]>=3.2.0

# Canvas parsing
beautifulsoup4>=4.12.0
requests>=2.32.0            # only needed if CANVAS_SOURCE=http

# HTML templates (for when Kenneth's templates land)
jinja2>=3.1.0
